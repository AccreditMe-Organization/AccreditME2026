name: AccreditMe CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  # ─────────────────────────────────────────────
  # Stage 1 — Repository structure validation
  # This job runs now while the project is being scaffolded.
  # It will be replaced by real backend/frontend jobs once
  # the NestJS and Angular projects are initialized.
  # ─────────────────────────────────────────────
  validate-repo:
    name: Validate Repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Confirm required files exist
        run: |
          echo "Checking repository structure..."
          test -f .gitignore && echo "✅ .gitignore found" || (echo "❌ .gitignore missing" && exit 1)
          test -f .github/workflows/ci.yml && echo "✅ ci.yml found" || (echo "❌ ci.yml missing" && exit 1)
          echo "✅ Repository structure check passed"

      - name: Confirm no secrets committed
        run: |
          echo "Checking for accidentally committed secret files..."
          ! test -f .env && echo "✅ .env not committed" || (echo "❌ .env file found in repo — remove it immediately" && exit 1)
          ! test -f .mcp.json && echo "✅ .mcp.json not committed" || echo "⚠️  .mcp.json found — ensure it contains no secrets"
          echo "✅ Secret file check passed"

  # ─────────────────────────────────────────────
  # Stage 2 — Backend (uncomment when backend/ exists)
  # ─────────────────────────────────────────────
  # backend:
  #   name: Backend — Lint, Type Check, Test
  #   runs-on: ubuntu-latest
  #   needs: validate-repo
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #         cache-dependency-path: backend/package-lock.json
  #     - name: Install dependencies
  #       run: npm ci
  #       working-directory: backend
  #     - name: TypeScript check
  #       run: npx tsc --noEmit
  #       working-directory: backend
  #     - name: Run unit tests
  #       run: npx jest --passWithNoTests
  #       working-directory: backend
  #     - name: Check migration status
  #       run: npx prisma migrate status
  #       working-directory: backend
  #       env:
  #         DATABASE_URL: ${{ secrets.DATABASE_URL }}

  # ─────────────────────────────────────────────
  # Stage 3 — Frontend (uncomment when frontend/ exists)
  # ─────────────────────────────────────────────
  # frontend:
  #   name: Frontend — Lint, Type Check, Build
  #   runs-on: ubuntu-latest
  #   needs: validate-repo
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #         cache-dependency-path: frontend/package-lock.json
  #     - name: Install dependencies
  #       run: npm ci
  #       working-directory: frontend
  #     - name: TypeScript check
  #       run: npx tsc --noEmit
  #       working-directory: frontend
  #     - name: Build Angular app
  #       run: npm run build
  #       working-directory: frontend

  # ─────────────────────────────────────────────
  # Stage 4 — Tenant isolation tests
  # (uncomment when backend tests exist)
  # ─────────────────────────────────────────────
  # tenant-isolation:
  #   name: Tenant Isolation Tests
  #   runs-on: ubuntu-latest
  #   needs: backend
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #         cache-dependency-path: backend/package-lock.json
  #     - name: Install dependencies
  #       run: npm ci
  #       working-directory: backend
  #     - name: Run tenant isolation tests
  #       run: npx jest --testNamePattern="should NOT return records belonging to a different tenant" --passWithNoTests
  #       working-directory: backend
  #       env:
  #         DATABASE_URL: ${{ secrets.DATABASE_URL }}
